package com.ylluberes.loantracker.domain.types;

public enum InterestType {
    //TODO Can structure this enum in such way every value can hold an expression of interest calculation
    SIMPLE, COMPOUND
}
